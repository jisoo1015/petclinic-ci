# ====================
# Nginx Web Server for PetClinic Frontend
# ====================
FROM nginx:1.25-alpine

# 기본 nginx 설정 제거 (ConfigMap으로 주입될 예정)
RUN rm -rf /etc/nginx/conf.d/*

# 정적 파일 복사
COPY web/startbootstrap-grayscale-gh-pages/ /usr/share/nginx/html/

# Welcome 이미지 복사
COPY web/image.png /usr/share/nginx/html/

# 헬스체크를 위한 기본 설정 추가 (ConfigMap이 없을 때를 대비)
RUN printf 'server {\n\
    listen 80;\n\
    server_name _;\n\
\n\
    location /health {\n\
        access_log off;\n\
        return 200 "healthy\\n";\n\
        add_header Content-Type text/plain;\n\
    }\n\
\n\
    location / {\n\
        root /usr/share/nginx/html;\n\
        index index.html;\n\
        try_files $uri $uri/ /index.html;\n\
    }\n\
}\n' > /etc/nginx/conf.d/default.conf

# 권한 설정
RUN chown -R nginx:nginx /usr/share/nginx/html && \
    chmod -R 755 /usr/share/nginx/html

EXPOSE 80

# Nginx 시작
CMD ["nginx", "-g", "daemon off;"]
